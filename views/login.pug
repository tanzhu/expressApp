extends layout

block content
    div.main.container
        div.left-side.col-xs-9
            p
                | 欢迎
        div.right-side.col-xs-3
            form(method='post',action='/login',enctype='application/json')
                div.form-group
                    label(for ='username')  用户名
                    input#userid.col-sm-12.form-control(type = 'text', name="username")
                div.form-group
                    label(for ='password')  密码
                    input#password.col-sm-12.form-control(type = 'password',name="password")
                div.form-group
                button#submit.col-sm-3.btn.btn-default(type = 'submit') 提交